{
  "name": "pochi-turn-worker",
  "private": true,
  "description": "Cloudflare Worker that mints short-lived TURN credentials for Pochi collab, with a monthly usage cap.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
